# Fixture Markdown

This is a small Markdown fixture used by the `UpstreamParityIT` parity harness
to exercise the per-type threshold override (`markdown = 0.75`).

## Sections

- A list of three items.
- Another item.
- A third item for good measure.

## Code

```python
def hello(name):
    print(f"Hello, {name}!")
```

## Links and emphasis

See [magika](https://github.com/google/magika) for the **upstream project**.
Inline `code` appears between backticks, and *italic* uses single asterisks.

> Blockquotes are fine too. They often carry attribution or a direct quote
> from another source.
